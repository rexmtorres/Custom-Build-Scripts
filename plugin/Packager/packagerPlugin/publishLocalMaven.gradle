apply plugin: 'maven'

repositories {
    mavenCentral()
}

group = publish.groupId
version = publish.publishVersion

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
        }
    }
}
